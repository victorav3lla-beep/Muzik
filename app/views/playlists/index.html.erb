<h1>Playlists#index</h1>
<p>Find me in app/views/playlists/index.html.erb</p>

<%= link_to "Create New PLaylist", new_playlist_path, class: "button"%>

<ul>
  <% @playlists.each do |playlist| %>
    <li><%= link_to playlist.title, playlist_path(playlist) %></li>
  <% end %>
</ul>
